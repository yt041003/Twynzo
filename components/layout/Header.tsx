import Link from 'next/link'; import LanguageSwitcher from './LanguageSwitcher'; import type {Locale} from '@/lib/i18n'; import {copy,segment} from '@/lib/i18n';
export default function Header({locale}:{locale:Locale}){const c=copy[locale],base=`/${segment(locale)}`;return <header className="bg-white/90 border-b border-slate-200 sticky top-0 z-30 backdrop-blur"><div className="container min-h-16 flex items-center gap-5"><Link className="wordmark mr-auto" href={`${base}/`}>Twynzo</Link><nav aria-label="Primary" className="hidden lg:flex gap-5 text-sm font-bold"><Link href={`${base}/would-you-rather/`}>{c.play}</Link><Link href={`${base}/would-you-rather-questions/`}>{c.questions}</Link><Link href={`${base}/would-you-rather-for-couples/`}>{c.couples}</Link><Link href={`${base}/funny-would-you-rather/`}>{c.funny}</Link><Link href={`${base}/hard-would-you-rather/`}>{c.hard}</Link><Link href={`${base}/would-you-rather/#daily`}>{c.daily}</Link></nav><LanguageSwitcher locale={locale}/></div></header>}
