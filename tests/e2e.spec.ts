import {test,expect} from '@playwright/test';test('English and Chinese SEO shells and counterpart link',async({page})=>{await page.goto('/en/funny-would-you-rather/');await expect(page.getByRole('heading',{level:1})).toContainText('Funny');await page.getByRole('link',{name:'繁中'}).click();await expect(page).toHaveURL(/zh-hant\/funny-would-you-rather/);await expect(page.getByRole('heading',{level:1})).toContainText('搞笑')});test('mobile game has tappable answers',async({page})=>{await page.setViewportSize({width:360,height:740});await page.goto('/en/would-you-rather/');await expect(page.getByRole('button',{name:/A:/})).toBeVisible()});
