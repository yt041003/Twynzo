import Link from 'next/link';import type {Locale} from '@/lib/i18n';import {segment} from '@/lib/i18n';
export default function Footer({locale}:{locale:Locale}){const b=`/${segment(locale)}`;return <footer className="mt-20 border-t bg-white"><div className="container py-10 flex flex-wrap gap-5 text-sm"><strong className="wordmark mr-auto">Twynzo</strong><Link href={`${b}/about/`}>{locale==='en'?'About':'關於我們'}</Link><Link href={`${b}/privacy/`}>{locale==='en'?'Privacy':'私隱政策'}</Link><Link href={`${b}/terms/`}>{locale==='en'?'Terms':'使用條款'}</Link><Link href={`${b}/contact/`}>{locale==='en'?'Contact':'聯絡我們'}</Link></div></footer>}
